OS.Classname = "bios";
OS.Name = "BIOS";
OS.Description = "Base for all OS"

function OS:Print(str, clr, skip_newline)
end

function OS:OnInstall()
end

function OS:Startup()
end

function OS:Tick()
end

function OS:Shutdown()
end

function OS:Command(com)
end